export function RemoteSyncExplainer() {
  return (
    <p className="mt-1.5 text-gray-600 text-xs leading-tight">
      Remote push is manual and in alpha. Your Signal is always preserved locally if push fails.
    </p>
  );
}
