{
  "name": "signal-vault",
  "private": true,
  "version": "0.0.0",
  "description": "Field-first intelligence and field-memory dApp for EVE Frontier",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:desktop": "pnpm --filter desktop tauri:dev",
    "build": "pnpm --filter web build",
    "build:desktop": "pnpm --filter desktop tauri:build",
    "typecheck": "pnpm --filter web typecheck",
    "typecheck:api": "pnpm --filter api typecheck",
    "typecheck:desktop": "pnpm --filter desktop typecheck",
    "lint": "pnpm --filter web lint",
    "test": "pnpm --filter web test",
    "test:run": "pnpm --filter web test:run",
    "test:api": "pnpm --filter api test:run",
    "test:scripts": "node --test scripts/world-api-env-policy.test.mjs",
    "check:lines": "node apps/web/scripts/check-line-limits.mjs",
    "check:prod-auth": "node scripts/assert-no-prod-dev-auth.mjs",
    "check:world-env": "node scripts/assert-world-api-env.mjs",
    "check:bundle-clean": "node scripts/assert-main-bundle-clean.mjs",
    "check:docs": "node scripts/assert-alpha-docs-current.mjs",
    "check:release": "pnpm typecheck && pnpm typecheck:api && pnpm test:run && pnpm test:api && pnpm test:scripts && pnpm build && pnpm check:prod-auth && pnpm check:world-env && pnpm check:bundle-clean && pnpm check:docs && pnpm check:lines",
    "check:desktop": "pnpm --filter desktop test:window-config && pnpm typecheck:desktop && pnpm build:desktop",
    "demo:status": "node scripts/print-alpha-demo-status.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
