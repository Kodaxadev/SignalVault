# Phase 00: Scaffold

## Goal

Create the base project with clean tooling, modular structure, and initial routes.

## Build

- Vite + React + TypeScript
- React Router
- Tailwind
- Vitest
- Testing Library
- Zod
- TanStack Query
- initial folder structure

## Deliverables

```txt
src/
  app/
  features/
  lib/
```

## Acceptance Criteria

- `pnpm install` succeeds.
- `pnpm build` succeeds.
- `pnpm test:run` succeeds.
- `pnpm typecheck` succeeds.
- No file exceeds 400 lines.
- `/app` route renders.
- `/ingame/object` route renders placeholder.
